bytes:218
- Imports React, ReactDOM, and the default `App` component.
- Creates the root on `#root` and renders `<App />` inside `React.StrictMode`.
- Bootstraps the test remote without extra application logic.