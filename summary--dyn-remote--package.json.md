bytes:806
- Package `dyn-remote` is private, uses ES modules, and ships `dev`, `build`, `lint`, and `preview` scripts.
- Dependencies are `react` and `react-dom`.
- Dev dependencies include Vite, React plugin, federation plugin, ESLint, and TypeScript tooling.
- Preview server runs on port `4201`.