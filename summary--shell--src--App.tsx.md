bytes:2774
- Shell `App` fetches remote config, registers remotes, loads modules, and renders them from state.
- Uses Axios plus federation helpers from `virtual:__federation__`; comments show mock-loading experiments.